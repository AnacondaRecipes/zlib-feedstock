anaconda_config_version: 1
upstream_sources:
  - webpage:
      url: https://zlib.net/fossils/
      regex: zlib-([\d.]+)\.tar\.gz
    packages:
      - libzlib
      - libzlib-wapi
      - zlib
      - zlib-wapi
